#ifndef STOP_H_
#define STOP_H_
using namespace std;

class Stop {
private:
string name;
float distanceToNext;

public:
    Stop(string name, float distanceToNext):name(name),distanceToNext(distanceToNext){}

    };

#endif
